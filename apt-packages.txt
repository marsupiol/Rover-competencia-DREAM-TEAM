# ==============================================================================
# Earth Rover Mini+ — System (APT) Package Dependencies
# ==============================================================================
# Base Image: osrf/ros:jazzy-ros-base
# Base OS: Ubuntu 24.04.4 LTS (Noble Numbat)
# 
# Nota sobre resolución de dependencias:
# El Dockerfile oficial utiliza `rosdep install --from-paths src --ignore-src -r -y`
# para resolver dinámicamente las dependencias de los paquetes ROS 2 en src/.
# Este archivo consolida tanto las herramientas base del sistema como los paquetes
# del ecosistema ROS 2 Jazzy requeridos para ejecución nativa/standalone.
# 
# Usage:
#   sudo apt-get update && grep -v '^#' apt-packages.txt | grep -v '^$' | xargs sudo apt-get install -y --no-install-recommends
# ==============================================================================
python3-pip
python3-colcon-common-extensions
python3-rosdep
python3-catkin-pkg
python3-empy
build-essential
cmake
git
pkg-config
libopencv-dev
python3-opencv
ros-jazzy-robot-localization
ros-jazzy-cv-bridge
ros-jazzy-tf2-ros
ros-jazzy-geographic-msgs
ros-jazzy-nav-msgs
ros-jazzy-sensor-msgs
ros-jazzy-geometry-msgs
ros-jazzy-std-msgs
