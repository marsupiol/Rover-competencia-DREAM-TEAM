# ==============================================================================
# Earth Rover Mini+ — AI & Deep Learning Stack Requirements (GPU Target)
# ==============================================================================
# CUDA / PyTorch Target:
# - No se encontró una versión de CUDA objetivo hardcodeada en el repo.
# - el índice de CUDA correcto cambia con cada release de PyTorch — antes de instalar, confirmá la combinación válida para torch==2.13.0 en https://pytorch.org/get-started/locally/ (seleccionar Linux + Pip + tu versión de CUDA) y usá el --index-url que esa página indique en ese momento, no lo copies de acá.
# - Python Version: Python 3.12.3
# - Base OS: Ubuntu 24.04.4 LTS (Noble Numbat)
# ==============================================================================

# Core Deep Learning Framework (GPU / CUDA)
torch==2.13.0
torchvision==0.28.0

# SAM-TP & GeNIE Path Planner (Instalados de forma explícita en Dockerfile con --no-build-isolation)
# -e src/third_party/sana-earth-rover-policy/genie
# -e src/third_party/sana-earth-rover-policy/traversability[hf]

# Configuration & Hydra Engine
hydra-core==1.3.5
omegaconf==2.3.1
antlr4-python3-runtime==4.9.3

# Classical ML & Scientific Computing
scikit-learn==1.4.2
scipy==1.14.1
numpy==1.26.4

# Vision & Hugging Face Hub
opencv-python-headless==4.9.0.80
pillow==12.3.0
huggingface_hub==1.28.0

# Supporting I/O & Progress Utilities
iopath==0.1.10
portalocker==4.1.0
tqdm==4.70.0

# ------------------------------------------------------------------------------
# Nota para instalación alternativa en CPU (sin GPU NVIDIA):
# pip install torch==2.13.0+cpu torchvision==0.28.0+cpu --index-url https://download.pytorch.org/whl/cpu
# ------------------------------------------------------------------------------
